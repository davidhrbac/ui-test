# OCP Node Driver

Provides:
- a component to create and edit Cloud Credentials associated with the `ocp` driver
- a component to configure `ocp` machine pools when creating or editing an RKE2 cluster
